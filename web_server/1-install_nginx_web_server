#!/usr/bin/env bash
# Install and configure Nginx to display "Holberton School" on the root page

# Update package list and install nginx
apt-get update -y
apt-get install -y nginx

# Create index page with the exact required text
echo "Holberton School" > /var/www/html/index.html

# Start nginx using service (not systemctl)
service nginx start
